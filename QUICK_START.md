# 🚀 Быстрый старт - Деплой на Netlify (БЕЗ MongoDB!)

## Вариант 1: Netlify Blobs (САМЫЙ ПРОСТОЙ - 5 минут)

### 1️⃣ Создайте GitHub репозиторий (2 минуты)

```bash
# В терминале проекта:
git remote add origin https://github.com/ВАШ_USERNAME/shorto.git
git branch -M main
git push -u origin main
```

Или создайте новый репозиторий на https://github.com/new и следуйте инструкциям.

### 2️⃣ Деплой на Netlify (3 минуты)

1. Войдите на https://app.netlify.com
2. **Add new site** → **Import an existing project**
3. Выберите **GitHub** → найдите репозиторий `shorto`
4. Netlify автоматически обнаружит настройки → **Deploy site**

### 3️⃣ Готово! 🎉

Ваш сайт доступен по адресу: `https://ваш-сайт.netlify.app`

**Никаких переменных окружения не требуется!**
**Никакой настройки БД не требуется!**

---

## Вариант 2: MongoDB Atlas (для больших проектов - 10 минут)

Если нужна более мощная БД:

### 1️⃣ Создайте MongoDB Atlas (5 минут)

1. Зарегистрируйтесь: https://www.mongodb.com/cloud/atlas/register
2. Создайте **FREE** кластер (M0)
3. Создайте пользователя БД:
   - Username: `shorto_user`
   - Password: (сохраните!)
4. Network Access → Add IP → **Allow Access from Anywhere** (0.0.0.0/0)
5. Скопируйте строку подключения:
   - Clusters → Connect → Connect your application
   - Замените `<password>` на ваш пароль

### 2️⃣ Переключитесь на MongoDB версию

```bash
# В терминале проекта:
mv netlify/functions/api.js netlify/functions/api-blobs.js
mv netlify/functions/api-mongodb.js netlify/functions/api.js
mv netlify/functions/redirect.js netlify/functions/redirect-blobs.js
mv netlify/functions/redirect-mongodb.js netlify/functions/redirect.js

git add .
git commit -m "Switch to MongoDB"
git push
```

### 3️⃣ Создайте GitHub репозиторий (если еще не создали)

```bash
git remote add origin https://github.com/ВАШ_USERNAME/shorto.git
git branch -M main
git push -u origin main
```

### 4️⃣ Деплой на Netlify (3 минуты)

1. Войдите на https://app.netlify.com
2. **Add new site** → **Import an existing project**
3. Выберите **GitHub** → найдите репозиторий `shorto`
4. Netlify автоматически обнаружит настройки → **Deploy site**

### 5️⃣ Настройте переменные окружения (1 минута)

1. В Netlify Dashboard вашего сайта:
2. **Site settings** → **Environment variables** → **Add a variable**
3. Добавьте две переменные:

   ```
   MONGODB_URI = mongodb+srv://shorto_user:ВАШ_ПАРОЛЬ@cluster.xxxxx.mongodb.net/?retryWrites=true&w=majority
   MONGODB_DB = shorto
   ```

4. **Save**
5. **Deploys** → **Trigger deploy** → **Clear cache and deploy site**

### 6️⃣ Готово! 🎉

---

## 📊 Сравнение вариантов

| Характеристика | Netlify Blobs | MongoDB Atlas |
|---------------|---------------|---------------|
| Настройка | Не требуется | 5 минут |
| Время деплоя | 5 минут | 10 минут |
| Бесплатный лимит | 1 GB | 512 MB |
| Скорость | Очень быстрая | Быстрая |
| Подходит для | Малые/средние проекты | Большие проекты |
| Расширенные запросы | Нет | Да |

## 🆘 Проблемы?

### Netlify Blobs

- Если данные не сохраняются, проверьте логи: Netlify Dashboard → Functions → Logs
- Убедитесь, что `@netlify/blobs` установлен в `package.json`

### MongoDB Atlas

- Проверьте, что IP 0.0.0.0/0 добавлен в Network Access
- Проверьте правильность пароля в строке подключения
- Убедитесь, что переменные окружения сохранены в Netlify

### 404 на коротких ссылках

- Проверьте, что `netlify.toml` загружен в репозиторий
- Trigger deploy заново

---

**Рекомендация:** Начните с Netlify Blobs (Вариант 1). Если понадобится больше возможностей, переключитесь на MongoDB.
