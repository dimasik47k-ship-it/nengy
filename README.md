# shorto. — URL Shortener

Современный сокращатель ссылок с аналитикой, дашбордом и API.

## ✨ Возможности

- 🔗 Быстрое сокращение ссылок
- 📊 Детальная аналитика (устройства, браузеры, ОС, геолокация, время)
- 📈 Визуализация данных (графики, диаграммы, тепловые карты)
- 🎯 Дашборд для управления ссылками
- 🔐 Система авторизации
- 🚀 API для интеграции
- 📱 Адаптивный дизайн
- 🌙 Темная тема

## 🛠 Технологии

- **Frontend:** Vanilla JavaScript, CSS3
- **Backend:** Node.js, Express.js (локально) / Netlify Functions (production)
- **Database:** MongoDB Atlas
- **Hosting:** Netlify

## 📦 Установка

### Локальная разработка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ВАШ_USERNAME/shorto.git
cd shorto
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env`:
```bash
cp .env.example .env
```

4. Настройте MongoDB Atlas (см. [MONGODB_SETUP.md](MONGODB_SETUP.md))

5. Запустите сервер:
```bash
npm start
```

6. Откройте http://localhost:3000

## 🚀 Деплой на Netlify

### Подготовка

1. Создайте аккаунт на [Netlify](https://app.netlify.com/signup)
2. Настройте MongoDB Atlas (см. [MONGODB_SETUP.md](MONGODB_SETUP.md))

### Деплой через GitHub

1. Создайте репозиторий на GitHub:
```bash
git remote add origin https://github.com/ВАШ_USERNAME/shorto.git
git branch -M main
git push -u origin main
```

2. В Netlify:
   - Нажмите "Add new site" → "Import an existing project"
   - Выберите GitHub и ваш репозиторий
   - Netlify автоматически обнаружит настройки из `netlify.toml`
   - Нажмите "Deploy site"

3. Настройте переменные окружения:
   - Перейдите в "Site settings" → "Environment variables"
   - Добавьте `MONGODB_URI` и `MONGODB_DB`
   - Trigger deploy

### Деплой через Netlify CLI

```bash
npm install -g netlify-cli
netlify login
netlify init
netlify deploy --prod
```

## 📚 API Документация

### Сокращение ссылки

```bash
POST /api/shorten
Content-Type: application/json

{
  "url": "https://example.com/very/long/url"
}
```

Ответ:
```json
{
  "short_url": "https://shorto.netlify.app/Xk9mQ2",
  "code": "Xk9mQ2"
}
```

### Массовое сокращение

```bash
POST /api/shorten/bulk
Content-Type: application/json

{
  "urls": [
    "https://example.com/url1",
    "https://example.com/url2"
  ]
}
```

### Получение статистики

```bash
GET /api/stats/:code
```

### Получение аналитики

```bash
GET /api/analytics
```

### Получение всех ссылок

```bash
GET /api/urls
Authorization: Bearer YOUR_TOKEN
```

## 📊 Структура проекта

```
shorto/
├── css/
│   └── style.css          # Стили
├── js/
│   └── script.js          # Frontend логика
├── images/                # Иконки и изображения
├── netlify/
│   └── functions/
│       ├── api.js         # API endpoints
│       ├── redirect.js    # Редирект коротких ссылок
│       └── db.js          # MongoDB утилиты
├── index.html             # Главная страница
├── server.js              # Express сервер (для локальной разработки)
├── netlify.toml           # Конфигурация Netlify
├── package.json
└── README.md
```

## 🔧 Конфигурация

### Переменные окружения

- `MONGODB_URI` - строка подключения к MongoDB Atlas
- `MONGODB_DB` - имя базы данных (по умолчанию: `shorto`)

### netlify.toml

Файл содержит настройки для Netlify:
- Путь к функциям
- Правила редиректов
- Публичная директория

## 📈 Аналитика

Для каждого клика сохраняется:
- IP адрес
- User-Agent
- Браузер
- Операционная система
- Тип устройства (Desktop/Mobile/Tablet)
- Referer
- Язык
- Временная метка

## 🤝 Вклад

Pull requests приветствуются! Для больших изменений сначала откройте issue.

## 📝 Лицензия

MIT

## 👤 Автор

Ваше имя

## 🙏 Благодарности

- MongoDB Atlas за бесплатный хостинг БД
- Netlify за бесплатный хостинг и serverless функции
